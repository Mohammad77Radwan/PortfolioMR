import React from 'react';

// Minimal Reveal animation wrapper
export const Reveal: React.FC<React.PropsWithChildren<{}>> = ({ children }) => {
  // You can add animation logic here later
  return <>{children}</>;
};
